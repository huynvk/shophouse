import { CircularProgress } from '@mui/material';
import { ExpandedBox } from '../Layouts';

const Loading = () => (
  <ExpandedBox>
    <CircularProgress />
  </ExpandedBox>
);

export default Loading;
