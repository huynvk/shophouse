import { CircularProgress } from '@material-ui/core';
import { ExpandedBox } from '../Layouts';

const Loading = () => (
  <ExpandedBox>
    <CircularProgress />
  </ExpandedBox>
);

export default Loading;
